const hobbies = ['Sports', 'Cooking'];

hobbies.push('Reading'); // the address of the array doesn't change

console.log(hobbies);

// Primitive values: numbers, strings, booleans & more (undefined)
// the value itself is stored

// Reference values: Objects - Arrays/Lists
// a pointer to the array/list has been stored in the memory
// if we use = to modify a value which means we change the value of that address
// use spread operator (...) in front of the object within a {} will create a new object
// so that it won't change the original object


